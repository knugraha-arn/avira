const nextConfig = {
  experimental: {
    nodeMiddleware: true,
  },
  images: {
    remotePatterns: [
      { protocol: 'https', hostname: 'qlmtwssjcostyddiwszc.supabase.co' },
    ],
  },
}

export default nextConfig
